{
  "scripts": {
   "build-app": "npm run build-client && npm install && npm start",
   "build-client": "cd frontend && npm run build-app && cd ..",
   "start": "pm2 start /backend/app.js -n Convertible9000 && pm2 monit"
  }
 }